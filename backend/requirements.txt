# KBID Proposal Generator — backend deps
fastapi==0.136.1
uvicorn[standard]>=0.34.0
python-multipart>=0.0.9

# config / validation
pydantic>=2.9

# database (self-hosted Postgres on the VPS)
psycopg[binary,pool]>=3.2

# auth — verify Clerk session JWTs
PyJWT[crypto]>=2.9.0
httpx>=0.27.0
cachetools>=5.3.0

# proposal + estimate documents
python-docx>=1.1.2
openpyxl>=3.1.5
