# Compose-level env (docker compose variable interpolation). Copy to `.env` on the VPS
# next to docker-compose.yml. App/Clerk config lives separately in backend/.env.
POSTGRES_USER=kbid
POSTGRES_PASSWORD=change-me-strong-prod-password
POSTGRES_DB=kbid
